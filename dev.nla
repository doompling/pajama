def_e print_int(int Int)
def_e print_bytes(byte_ptr BytePtr, int Int)

class Str
   buffer BytePtr
   length Int
   max_length Int
end

def main
   some_str = "lets goo!!"
   print_bytes(some_str.buffer, some_str.length)
end
